Saturday, 22 August 2026: Version 3.2
	bugfix: deleting an image while shuffle mode was active corrupted the shuffle index array (os_move_memory was passed an element count where a byte count - missing the sizeof(_viv_playlist_t *) factor - was required), leaving a stale pointer to the freed node; the next navigation dereferenced it (use-after-free / random crash, wrong image shown). The move now multiplies by sizeof; a regression test (tests/test_playlist_delete.c) reproduces the faulty element-count behaviour and verifies the fixed byte-count behaviour.
	bugfix: the status-bar pixel RGB was read from an uninitialized COLORREF when the cursor was outside the image (undefined behaviour, random pixel values); it is now initialized to 0 before the optional read.
	bugfix: _viv_frame_prev could wrap to frame index -1 while an animation was still loading (loaded_count == 0), indexing _viv_frames[-1]; the wrap is now clamped to frame 0.
	bugfix: uninstalling a file association used RegDeleteKey, which fails on keys that still have subkeys (eg: shell\open\command), leaving the association installed; switched to SHDeleteKeyW which removes the whole tree.
	changed: Everything IPC reply parsing (the RANDOM and OPEN/ADD WM_COPYDATA handlers) was duplicated ~160 lines; extracted a shared _viv_everything_parse_item() that keeps every trust-boundary check (cbData, data_offset, remaining, MAX_PATH, SIZE/CREATED/MODIFIED fields).
	changed: Everything IPC query sending (browse vs random) was duplicated ~100 lines; extracted _viv_send_everything_query() parameterised by reply message / offset / max results. The random path's debug print no longer dereferences the query pointer in release builds.
	changed: the slideshow rate radio-checks and the rate->command-id lookup now use the existing _viv_slideshow_rate_presets[] table instead of hand-written case lists, so adding a preset touches one table instead of four places.
	removed: dead files src/crt.c and src/crt.h (never compiled, contents mostly commented out, no include sites).
	removed: 84 tracked build artifacts from git (79 .obj files, vc145.pdb, .tlog, .lastbuildstate, FileListAbsolute.txt, test_images/heif_dec_test.obj) - they are reproducible build output and already covered by .gitignore.
	removed: stale workspace artifacts (test_left_drag.obj, four v3.0 installer executables shipped under nsis/).
	fixed: version resource FILEVERSION/PRODUCTVERSION numeric fields still read 1,0,0,16 while the string values said 3.1.0.0, so tools reading the fixed file info showed an ancient version; both are now 3.2.0.0.
	fixed: nsis/version.nsh still branded 3.0; now 3.2.
	fixed: README referenced a non-existent build_vs2019.bat and a wrong output directory; now documents build_release_both.bat and the real output paths.
	fixed: RELEASE-NOTES-3.1.md claimed 12 UI languages where the project ships 10; corrected.
	regression: x64 and Win32 Release builds compile clean (/W3, no warnings); tests/test_left_drag.exe 16/16 and tests/test_playlist_delete.exe 3/3 pass.

Saturday, 22 August 2026: Version 3.1
	fixed: dragging the image in borderless mode always moved the whole window - even when zoomed past the viewport or maximized (dragging a maximized window restored it and dragged it away). Left-drag now pans the image whenever it renders larger than the viewport, in any window state.
	added: smart left-drag rule shared by all scroll-type click actions (0=scroll, 5=1:1 scroll, 6=move-window): pan when the image overflows the viewport; move the window only while it fully fits a normal borderless window; do nothing on a maximized window so it can no longer be accidentally dragged out of maximize.
	changed: decision logic extracted into src/left_drag.h as a pure function; no new settings and no localization changes.
	added: unit tests for the drag-decision truth table (tests/test_left_drag.c, exhaustive 16 cases) and a SendInput-based end-to-end drag harness (tests/e2e_drag_test.ps1 + make_test_image.ps1) that simulates real drags and asserts window rect/maximized state; verified red against the pre-fix binary and green after.

Tuesday, 18 August 2026: Version 3.0
	major repository cleanup and hardening pass.
	removed: 6000+ redundant files from the repository (build output, tar-extraction garbage, the bundled CMake toolchain, Visual Studio caches, stale installers and test executables).
	removed: unused libwebp modules (encoder, command-line tools, SWIG bindings, WebP-to-JS/WASM, tests and docs) - only the decoder needed by the viewer is kept; compile units drop from 88 to 64.
	removed: vcxproj dead configurations (vcxproj shrinks from 1669 to 490 lines) - 16 build configurations that could never compile (missing rescpver.exe, regex/pcre headers, minicrt.lib/HTMLHelp.lib) plus all ARM configurations and the Win98-era UnicoWS.lib link dependency.
	removed: ~500 lines of dead code (unused functions, commented-out blocks, fossil macros, orphaned localizations) across the src tree.
	fixed: leftover references to removed code in comments (dead tooltip subsystem calls, unused _viv_is_alt variable).
	added: version is now branded 3.0 (installer filenames use 3.0; Windows file properties use 3.0.0.0).
	regression: all 16 supported image formats (incl. HEIC/HEIF, animated WebP/GIF) pass the load-and-render test suite on both x86 and x64.

Saturday, 16 August 2026: Version 2.6
	fixed: x64 builds crashed while decoding HEIC/HEIF (image displayed as garbage): the MSVC x64 optimizer at /O2 miscompiles libde265's scan-order table initialization (fill_scan_pos never finds the target cell and walks the sub-block index negative, faulting in init_scan_orders). The x64 libde265 static library is now built with /O1; x86 and x64 decode to byte-identical pixels (verified by SHA256 over full decoded frames). The x86 library is unaffected.
	fixed: the WIC loader ignored a CopyPixels failure in the direct 32bppBGRA path and delivered an uninitialized pixel buffer to the frame pipeline (garbled image on WIC decoder errors).
	added: HEIC/HEIF decoding is now built in via libheif + libde265 (static, MIT/LGPL). Previously HEIC/HEIF relied on the Windows Imaging Component, which needs the store "HEIF Image Extensions" package that most systems don't have - HEIC files showed a "failed to load image" error. The built-in decoder works everywhere, no system components required, and EXIF orientation is parsed from the file (the Windows property store can't read HEIC EXIF).
	added: HEIC/HEIF re-added to the open-file dialog, Everything search filters, file associations, command line switches and the NSIS installer options.
	fixed: crash (segfault) while parsing EXIF orientation from HEIC files whose TIFF offset is non-zero (out-of-bounds read in the TIFF IFD walk).
	fixed: image loading test harness mis-detected the "failed to load image" message box as a successful render (it counted the grey dialog background as non-black pixels).
	fixed: frame skipping could hang the UI forever on animations with zero-delay frames (animated webp) or while the animation was still loading.
	fixed: _viv_get_mipmap used uninitialized struct fields (crash) when GDI handles were exhausted.
	fixed: multi-frame images without a frame-delay property (multi-page TIFF, multi-frame ICO) could crash on load (uninitialized size / NULL dereference).
	fixed: Everything IPC replies lacked COPYDATA buffer validation - a malformed message from any local process could crash the viewer or cause out-of-bounds reads.
	fixed: command line parsing could overflow the stack with very long arguments (string_get_word is now length-bounded).
	fixed: os_mirror_region read uninitialized memory on empty regions (RTL mirrored painting).
	fixed: webp images with EXIF orientation 5-8 were displayed with a wrong aspect ratio.
	fixed: real drag & drop leaked the shell HDROP (missing DragFinish; clipboard paste no longer fakes a drop message).
	fixed: Everything search requested the date-modified field when only file size was indexed.
	fixed: string_copy_utf8_string / _config_write_string could leave buffers unterminated when truncated.
	fixed: small_pool_kill left dangling chunk pointers; os_kill left dangling module handles and custom colors pointer.
	fixed: Debug|x64 configuration failed to compile (missing /utf-8).
	fixed: NSIS English installer failed to build (self-referencing INSTALL_OPTIONS2_FILE macro).
	fixed: misc (key list trailing comma added a spurious key, debug format %s/%S mismatch in os_get_orientation, load thread created a stream from a half-read buffer when terminated).

Monday, 10 August 2026: Version 1.0.0.16
	code review and hardening pass.
	fixed: removed claimed-but-unimplemented format support (HEIC/HEIF, AVIF, APNG, TGA, WMF/EMF, CUR/ANI) from the file dialog and Everything search filters; README updated to match actual support.
	fixed: image load thread termination checks were commented out; restored so switching images cancels the previous load.
	fixed: buffer overflow when copying long paths into WIN32_FIND_DATA.cFileName (use MAX_PATH-bounded copy).
	fixed: GetLayout was loaded from user32.dll instead of gdi32.dll, breaking RTL mirroring.
	fixed: vcxproj referenced 5 non-existent render*.h headers and compiled empty render*.c stubs; removed.
	fixed: NSIS installer hardcoded "InstallOptions2.ini" instead of the localized file macro.
	fixed: single-instance startup race (retry FindWindow for ~1s).
	fixed: _viv_load_image_terminate now uses interlocked operations.
	fixed: large files > 2GB are refused instead of truncating (GetFileSizeEx).
	fixed: debug mem_free_debug NULL check order.
	fixed: playlist shuffle index array leak when re-shuffling.
	fixed: reply queue leak when PostMessage fails during shutdown.
	fixed: Everything IPC reply parsing missing bounds checks.
	fixed: /install-options now whitelists allowed switches.
	fixed: SetThreadExecutionState/CoInitializeEx failure handling.
	fixed: mipmap selection compared render_high against mip_wide instead of mip_high.
	fixed: %f formatting used float precision; now rounds via double.
	fixed: CreateThread failure now reports a load failure instead of hanging.
	fixed: removed VS2005-era library paths and full-disk vcvarsall scan from build scripts.
	fixed: misc (duplicate localization_init call, MF_DELETE misuse, RC copyright mojibake, version info, UTF-8 BOM script idempotency).

Monday, 12 January 2026: Version 1.0.0.15
	fixed an issue with scrolling large images.
	improved position offset when using numpad keys.

Tuesday, 16 December 2025: Version 1.0.0.14
	fixed an issue with webp using the wrong background color.
	fixed an issue with tracking the center of the image when resizing.
	fixed an issue with rendering images larger than 65536x65536.
	fixed an issue with screen saver not being prevented in slide show mode or when there is an animation.
	improved rendering performance.
	improved zooming of small images when going fullscreen.
	move window action will now scroll if zoomed.
	re-enabled WEBP_MSC_SSE41 
	added title_bar_format ini setting.
	added System.Photo.Orientation support.
	added - command line switch support. Escape with "" or include a . in your switch.

Sunday, 10 August 2025: Version 1.0.0.13
	fixed an issue with losing the image center on resize
	fixed back/forward mouse buttons not working from toolbars
	fixed an issue with loading the wrong next image when it was preloaded and changed externally
	improved rendering performance
	added left click action: move window
	added auto size window GUI option

Friday, 25 July 2025: Version 1.0.0.12
	fixed a gdi leak when freeing mipmaps
	fixed a gdi leak when refreshing
	fixed an issue with loading the wrong next image after changing the sort
	added natural sort

Sunday, 20 July 2025: Version 1.0.0.11
	fixed an issue with using the wrong association registry key
	fixed a crash when interrupting the loading of a multi-frame image
	fixed an issue with displaying images after rotating
	fixed an issue with remembering some view settings
	fixed an issue with losing the view after toogling fullscreen
	inverted increase/descrease slideshow rate
	improved preloading next/previous image
	added cache last image

Tuesday, 17 June 2025: Version 1.0.0.10
	improved webp memory usage

Thursday, 22 May 2025: Version 1.0.0.9
	added webp support
	added animated webp support
	added mipmap support for faster resizing
	added preload option
	added maximized options
	added option to copy image
	added option to copy filename
	added new code signing certificate: voidtools PTY LTD
	improved animation timings

Wednesday, 11 October 2017: Version 1.0.0.8b
	added ctrl mousewheel action
	fixed an issue when only one image was in the playlist
	fixed a leak when loading an image
	fixed a crash when there was only one shuffled image
	improved randomize seeding
	fixed a leak when loading an image while already loading an image
	
Wednesday, 20 September 2017: Version 1.0.0.7b
	added randomized everything search option
	added 1:1 scroll option
	added rotate options
	fixed a bug when navigating to the next image from the first image

Thursday, 14 September 2017: Version 1.0.0.6b
	added /x /y /width /height /minimal /compact /shuffle /everything command line options.
	added jump to list
	added everything search
	added shuffle
	fixed /dc command line option
	fixed a bug when opening void image viewer and getting a random filename
	
Thursday, 31 August 2017: Version 1.0.0.5b
	fixed a bug with move to/copy to
	fixed a bug with save as filters

Saturday, 19 August 2017: Version 1.0.0.4b
	added background color selection
	added close
	added status bar
	added keyboard and mouse customization
	added print
	added play gifs at least once option
	added set as desktop background
	added prevent sleep when playing or animating
	added custom slideshow rate
	added animation menu
	added copy to / move to menu
	added automatic window sizes
	added multiple instances / single instance
	improved portability
	fixed an issue with holding a handle to the current directory

Saturday, 18 Feburary 2017: Version 0.2.0.3b
	added Multiple instances support
	added multiple file drag drop support
	added command line options /slideshow /fullscreen /window /rate <milliseconds> and /?
	added Edit and Preview file menu items

Saturday, 17 August 2016: Version 0.0.0.1b
	added animation controls
	added mouse back/forward
	added min/mag filter modes
	added file associations
	added open folders support
	added Open Location
	added properties option
	added middle mouse move
	added IStream support
	added navigation
	added hide cursor on fullscreen
	improved slideshow mode
	improved 1:1 mode
	fixed an issue with transparent gifs
	fixed an issue when deleting an image and displaying the next image
